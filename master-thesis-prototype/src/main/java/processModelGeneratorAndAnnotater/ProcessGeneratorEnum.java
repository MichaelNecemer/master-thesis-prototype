package processModelGeneratorAndAnnotater;

public class ProcessGeneratorEnum {
	
	public static enum NodeType{
		TASK, XOR_SPLIT, XOR_JOIN, PARALLEL_SPLIT, PARALLEL_JOIN, START_EVENT, END_EVENT
	}

}
